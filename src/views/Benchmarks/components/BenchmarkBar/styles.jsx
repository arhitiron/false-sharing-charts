import React from 'react';

export const styles = theme => ({
    root: {},
    item: {
        display: 'flex',
        flexDirection: 'column',
        marginBottom: theme.spacing(5)
    },
    chart: {}
});